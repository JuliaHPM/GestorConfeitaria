import { styled } from "..";

export const ImageContainer = styled('div', {
    overflow: 'hidden',
    height: 45,
    width:45,
    // margin: '0 1rem',
    borderRadius: 50,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    // position: 'relative',
})