import { styled } from "..";

export const SubTitle = styled('div', {
    fontWeight: '$light',
    fontSize: '$xMedium',
    marginTop: 15,
    marginBottom: 0,
    textAlign: 'left',
    color: '$gray400',
    marginBottom: 15,
    marginTop: 0,
    whiteSpace: "pre-line"
})

export const ImageContainer = styled('label', {
    position: 'relative',

    div: {
        overflow: 'hidden',
        width: 450,
        height: 350,
        position: 'relative',

    },
})







